{
  "name": "auto-branch",
  "version": "1.0.0",
  "private": true,
  "description": "Simple GitHub application to create desired branches and apply branch protection rules upon repository creatory.",
  "author": "Iresh Dissanayaka <socialexpz1@gmail.com>",
  "license": "ISC",
  "repository": "https://github.com/IreshMM/auto-branch.git",
  "homepage": "https://github.com/IreshMM/auto-branch",
  "bugs": "https://github.com/IreshMM/auto-branch/issues",
  "keywords": [
    "probot",
    "github",
    "probot-app"
  ],
  "scripts": {
    "dev": "nodemon",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "probot": "^9.5.3"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "nock": "^11.4.0",
    "nodemon": "^2.0.0",
    "smee-client": "^1.1.0",
    "standard": "^14.3.1"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "nodemonConfig": {
    "exec": "npm start",
    "watch": [
      ".env",
      "."
    ]
  },
  "jest": {
    "testEnvironment": "node"
  }
}
